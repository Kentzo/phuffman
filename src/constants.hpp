#pragma once
#ifndef CONSTANTS_H_
#define CONSTANTS_H_

namespace phuffman {
    namespace constants {
        const uint16_t ALPHABET_SIZE = 256;
        const uint16_t MAXIMUM_CODELENGTH = 48; // bits
        const size_t MAXIMUM_DATABLOCK_SIZE = 32951280098; // bytes
    }
}

#endif /* CONSTANTS_H_ */
